#include <iostream>
using namespace std;
int main () {
  cout << "Введите количество тварп\n";
  int travp;
  while (!(cin >> travp) ){
    cin.clear();
    while (cin.get() != '\n' && cin.get() <= 0){
      cout << "Ведено не верное значение\n";
    }
  }
  int nalog;
  double result = 0.0;
  if (travp <= 5000){ cout << "0%" << endl;}
  for (int x = 5000 ; travp == 0 || x > 35000; x = x * 2)
  double i = 0.1;
  double i = i + 0.05;
  double ti;
  travp = ti;
  double y;
  int x = y;
  double result = (ti - y)*i;
  result = result + result;
  }
  cout << result << endl;

}
